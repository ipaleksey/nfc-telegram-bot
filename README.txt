# NFC → Telegram VIP БОТ (пошагово для новичка)

## 0) Что внутри архива
- `bot.py` — сам бот
- `.env.example` — образец настроек (переименуйте в `.env`)
- `requirements.txt` — список зависимостей

## 1) Установите Python
- Скачайте Python 3.11 с https://www.python.org/downloads/windows/
- Установите, поставив галочку **Add Python to PATH**.

## 2) Создайте папку проекта
- Например: `C:\NFCBot`
- Распакуйте сюда архив.

## 3) Создайте виртуальное окружение и установите зависимости (Windows)
Откройте **Командную строку** (Win+R → cmd) и выполните:
```
cd C:\NFCBot
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
```

## 4) Создайте бота в Telegram
- Откройте `@BotFather` → `/newbot` → придумайте имя и username.
- Скопируйте **Bot Token** (выглядит как `123456:ABC...`).

## 5) Создайте закрытый раздел (канал/чат)
- Создайте **приватный канал** в Telegram (например, “83 Owners Club”).
- Добавьте **вашего бота** в канал и дайте ему **права администратора**.

## 6) Узнайте TARGET_CHAT_ID (ID канала)
Способ простой:
1. Напишите что‑нибудь в канале.
2. Перешлите это сообщение боту `@getidsbot` или `@RawDataBot` — он пришлёт Chat ID.
3. Возьмите значение вида `-100xxxxxxxxxxxx` — это и есть `TARGET_CHAT_ID`.

## 7) Узнайте свой Telegram user ID (для ADMINS)
- Напишите в `@userinfobot` — он ответит вашим `id` (число).
- Запишите это число в `ADMINS` (можно несколько через запятую).

## 8) Настройте .env
- Переименуйте `.env.example` → `.env`
- Откройте в Блокноте и заполните значения:
```
BOT_TOKEN=ваш_токен_из_BotFather
TARGET_CHAT_ID=-100... (из шага 6)
ADMINS=ваш_user_id (из шага 7)
INVITE_TTL_MINUTES=10
```

## 9) Запустите бота
В той же консоли (где активировано окружение):
```
python bot.py
```
Если всё ок, увидите строку вида: `Bot @YourBot started. Target chat: -100...`

## 10) Сгенерируйте коды и ссылки для NFC
В Telegram напишите вашему боту (с админ-аккаунта):
```
/gen 20 PASSPORT-COVER
```
Бот ответит списком из двух колонок: **код** и **ссылка**.
Пример:
```
ABCD1234EFGH    https://t.me/YourBot?start=ABCD1234EFGH
```

## 11) Запишите ссылку на NFC‑наклейку
- Установите приложение **NFC Tools** или **NXP TagWriter** на телефон.
- Откройте → Раздел **Write/Запись** → **URL/URI**.
- Вставьте ссылку из ответа бота (правый столбец).
- Нажмите **Write/Записать** и поднесите метку к телефону.
- Готово!

💡 Советы:
- Для iPhone важно использовать метки **NDEF** (обычно NTAG213/215/216).
- Если метка «пустая», в приложении есть пункт **Format/Форматировать (NDEF)**.
- Ссылка должна вести ровно на `https://t.me/<username_бота>?start=<код>`.

## 12) Проверка
- Приложите телефон к метке → откроется ваш бот с параметром.
- Бот привяжет код к пользователю и выдаст **одноразовую** ссылку-приглашение в канал.
- Повторный доступ: команда `/access` в чате с ботом.

## Частые проблемы
- **Bot is not a member of the channel** — добавьте бота в канал и дайте права администратора.
- **Set BOT_TOKEN and TARGET_CHAT_ID in .env** — проверьте `.env` рядом с `bot.py`.
- **Invite link creation failed** — у бота недостаточно прав в канале.

Удачи! Если нужно — помогу подключить несколько каналов, выгрузку кодов в Excel, брендированные тексты и пр.
